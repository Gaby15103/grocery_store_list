services:
  postgres:
    image: postgres:16
    container_name: postgres_grocery_list
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - postgresDb:/var/lib/postgresql

volumes:
  postgresDb:
